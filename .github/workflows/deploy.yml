name: 🚀 Despliegue automático a Hostinger

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Descargar el repositorio
        uses: actions/checkout@v3

      - name: 📦 Instalar pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: 🧹 Limpiar dist y caché
        run: rm -rf dist node_modules/.tmp
        working-directory: frontend

      - name: 🔧 Instalar dependencias
        run: pnpm install --no-frozen-lockfile
        working-directory: frontend

      - name: 🏗️ Construir la aplicación
        run: pnpm build
        working-directory: frontend

      - name: 📂 Verificar build
        run: ls -la frontend/dist

      - name: 🔐 Probar login FTP (diagnóstico)
        run: |
          sudo apt-get update -y
          sudo apt-get install -y lftp
          echo "Probando conexión a $FTP_SERVER como $FTP_USERNAME";
          set +e
          lftp -u "$FTP_USERNAME","$FTP_PASSWORD" -e "cls -1; bye" "$FTP_SERVER" || exit 1
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}

      - name:  Subir a Hostinger por FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}            # Debe coincidir exactamente (ej: reformix)
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp                                    # Sólo cambiar a ftps si activas TLS explícito
          local-dir: frontend/dist/                        # Contenido compilado
          server-dir: public_html/                         # Raíz de tu dominio para este usuario
          exclude: |
            **/.git*
            **/node_modules/**
            **/*.map
            **/story*/**
          log-level: verbose
          dangerous-clean-slate: true                      # Borra archivos remotos que no existan local (confirma antes)