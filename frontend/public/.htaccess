# =============================================================
# Archivo .htaccess para hosting Apache
#
# Permite que una Single Page Application (SPA) hecha con React/Vite
# funcione correctamente en Hostinger u otro hosting con Apache.
# - Redirige todas las rutas al index.html para el routing del cliente
# - Opcional: fuerza HTTPS si el hosting lo soporta
# - Sirve archivos estáticos si existen
# =============================================================

RewriteEngine On
# Si el archivo o directorio real existe, servirlo normalmente
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]
# Cualquier otra ruta entregar index.html para permitir routing del cliente
RewriteRule ^ index.html [L]
